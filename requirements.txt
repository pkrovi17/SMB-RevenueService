Flask==2.3.3
pandas==2.1.4
plotly==5.17.0
prophet==1.1.4
numpy==1.26.2
Werkzeug==2.3.7
openpyxl==3.1.2
python-dotenv==1.0.0

# Note: If you encounter TBB "already found in load path" errors,
# try installing prophet with: pip install prophet --no-deps
# Then install dependencies manually: pip install cmdstanpy pystan 